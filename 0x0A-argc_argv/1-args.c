#include <stdio.h>

/**
 * main - Entry point to the program
 * @argc: Argument count
 * @argv: Argument vector
 * Return: 0 always
 */
int main(int argc, char *argv[])
{
	printf("%i\n", argc - 1);
	return (0);
}
