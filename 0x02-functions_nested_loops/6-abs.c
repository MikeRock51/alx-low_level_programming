#include "main.h"
/**
 * _abs - calculates the absoluye value if an integer
 * @j: takes an int j as input
 *
 * Returns: 0 always
 */
int _abs(int j)
{
	int i;
	int v;

	for (i = 0; i <= j; i++)
	{
		v += i;
	}

	return (v);
}
