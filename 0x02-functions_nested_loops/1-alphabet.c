#include "main.h"
/**
 * print_alphabet - The entry point of the program
 *
 * Return: 0 if code works
 */
void print_alphabet(void)
{
	for (int i = 'a'; i <= 'z'; i++)
	{
		_putchar(i);
	}

	_putchar('\n');
}
